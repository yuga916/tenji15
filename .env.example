# データソース切り替え: mock | official
DATA_SOURCE_MODE=mock

# ---- 静的サイト生成 ----
# 本番URL(canonical / sitemap / JSON-LD 用) 例: https://<user>.github.io/tenji15
# ※ ページ内リンクは相対パス生成のため、ローカルで直接HTMLを開いても表示できます
SITE_URL=https://example.github.io/tenji15
